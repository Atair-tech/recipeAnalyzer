# Desktop Shell Placeholder

This folder is reserved for a future Tauri shell.

The current rule is simple:

- keep domain logic in `backend/`
- keep UI logic in `frontend/`
- add only desktop-specific code here later

That avoids mixing platform concerns into the recipe domain too early.
