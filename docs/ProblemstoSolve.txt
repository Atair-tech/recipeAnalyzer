1. 问题：在自然语言搜索页面执行检索后，只显示 10 个结果，而不是全部结果。需求：支持每页显示 10 个，可以翻页；也支持把结果导出成 Excel。
解决情况：已解决。
- 自然语言搜索接口已支持分页参数 `limit` / `offset`。
- 前端 AI 页面已增加上一页、下一页、总数和当前区间显示。
- 已增加“导出 Excel”按钮，可导出当前查询的完整结果集。

2. 问题：分析页面是固定的指标，没法根据用户想要的标签、规则进行统计。需求：像一个正常的数据分析 dashboard 一样，默认显示一个总览的数据统计，然后可以根据用户想要的数据（可以是下拉选单的方式）给出图表。
解决情况：已解决。
- 分析页已改为“总览卡片 + 可配置图表”模式。
- 当前支持按下拉选项切换统计维度，例如专题、分组、菜系、亚菜系、状态、标签、食材。
- 当前支持按下拉选项切换统计范围，例如全部、正式菜谱、待办项、BMD、CC、仅有做法、无做法。

3. 问题：菜谱库页面的筛选选单都堆叠在一起了，看不清楚。需求：重新设计该页 UI。
解决情况：已解决。
- 菜谱库筛选区域已重构为独立筛选卡片。
- 筛选项已按网格布局分组展示，避免原先堆叠和重叠。
- BMD / CC 选项已拆成更清晰的开关区域。

4. 需求：请支持按照菜谱库的标签进行筛选后，导出为 Excel。
解决情况：已解决。
- 菜谱库接口已支持按标签筛选。
- 菜谱库页面已增加标签筛选下拉框。
- 已增加“导出结果”按钮，可导出当前筛选条件下的菜谱结果为 Excel。
说明：当前数据库中的标签数据量仍然较少；如果后续导入或人工确认更多标签，这个筛选和导出能力会更有价值。

5. 问题：现在菜谱库中的“原始导入文本”已经不是原来 Excel 里的内容了。需求：请在菜谱库中，将“原始导入文本”做成一个折叠的部分，默认折叠起来，点击后再显示出文字，再点击也可以收起。展开后，显示原本在 `recipes.xlsx` 中的内容。
解决情况：已解决。
- 菜谱详情页已将“原始导入文本”改为折叠区域，默认收起。
- 展开后显示的是根据 `raw_import_rows` 还原出的原始 Excel 内容，而不是后续结构化后的展示文本。
- 再次点击可收起。

补充说明
- 本轮修改已覆盖前后端和导出链路。
- 导出接口的文件名编码问题已修复，避免中文查询条件导致导出报错。
