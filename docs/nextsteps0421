# 2026-04-21 下一步开发建议

这份文档用于记录当前阶段之后最值得推进的事项和建议步骤。

当前判断：程序已经完成了基础平台搭建，下一步不应优先继续堆页面，而应优先提升：
- 数据质量
- AI 可靠性
- 运维可见性
- 可交付性

## 一、当前开发进度判断

已经比较稳定的部分：
- Excel 到 SQLite 的差异同步
- 专题库 / 分组 / 配对审查
- 菜谱库浏览、筛选、导出
- 自动标签体系与批量打标
- 本地 Ollama 接入与智能问答
- AI 精校任务体系
- 数据分析、数据库浏览、导入历史、AI 日志

当前主要短板：
- 结构化食材仍然是整个系统质量的底层瓶颈
- 自动标签已经能跑，但审核机制不足
- 智能问答已经成型，但检索稳定性还不够强
- AI 任务分散在不同页面，缺统一的运维视图
- 最终交付体验还没有收尾

## 二、下一步最值得做的事项

### 1. 结构化食材审查页

优先级：最高

原因：
- 直接影响筛选、分析、自动标签、智能问答
- 当前 AI 精校已经能跑，但还缺“结果审查和纠偏闭环”

建议步骤：
1. 新建 `管理 -> 结构化食材审查`
2. 展示原始食材文本
3. 展示当前结构化食材
4. 展示 AI 精校前后差异
5. 支持人工标记：
   - 正确
   - 错误
   - 需要重跑
6. 支持单条 recipe 重跑精校
7. 将高频人工修正沉淀为规则，而不是长期依赖 prompt

目标：
- 把“数据脏问题”从被动排查，变成可管理、可回溯、可持续收敛的流程

### 2. 自动标签审核闭环

优先级：高

原因：
- 自动标签已经有产出，但缺少后续治理能力
- 如果没有审核机制，标签体系会越来越难控

建议步骤：
1. 在 `标签管理` 中增加标签命中记录查看
2. 支持查看“某个标签命中了哪些菜”
3. 支持人工：
   - 接受
   - 拒绝
   - 删除标签关联
4. 记录标签命中的来源：
   - 规则
   - AI
   - 人工

目标：
- 让自动标签从“模型结果”变成“可维护的数据资产”

### 3. 智能问答检索稳定性增强

优先级：高

原因：
- 智能问答链路已经打通
- 但效果上限仍然受概念解释稳定性影响

建议步骤：
1. 建立本地概念词典
   - 例如：
     - 病号菜 -> 清淡 / 易消化 / 蒸煮 / 少刺激
     - 下饭 -> 酱香 / 米饭搭配 / 重口
2. 将规则词典与 LLM 的 expanded terms 合并进入检索
3. 对常见模糊查询建立可维护的兜底规则

目标：
- 即便本地模型较弱，智能问答也能保持可用下限

### 4. AI 任务中心

优先级：中高

原因：
- 目前 AI 精校、自动标签、问答日志分别散落在不同页面
- 任务状态与错误排查不够集中

建议步骤：
1. 建立 `管理 -> AI 任务中心`
2. 统一展示：
   - 自动标签任务
   - AI 精校任务
   - 智能问答日志摘要
3. 展示：
   - 运行中 / 已暂停 / 失败 / 已完成
   - 模型
   - 进度
   - 错误数
   - 最近处理对象

目标：
- 提升 AI 任务的可观测性和维护效率

### 5. 最终交付收尾

优先级：中

建议在前几项稳定后再做。

建议步骤：
1. 一键启动 / 停止
2. 避免黑窗
3. 备份与恢复
4. 首次使用说明
5. Tauri 桌面壳接入

目标：
- 把当前开发态工具收束成可交付给家庭用户使用的产品

## 三、推荐实际推进顺序

建议按以下顺序推进：

1. 结构化食材审查页
2. 自动标签审核闭环
3. 智能问答检索稳定性增强
4. AI 任务中心
5. 交付收尾

## 四、如果只做一个下一步

如果只选一个，我建议直接做：

`结构化食材审查页 + AI 精校差异审查`

原因：
- 这是当前整个系统最核心的质量瓶颈
- 它会同时提升：
  - 菜谱库筛选
  - 数据分析
  - 自动标签
  - 智能问答

## 五、执行原则

后续推进时建议坚持：
- 优先做“质量闭环”，再做“新功能”
- 优先做“可审查、可回溯、可增量”的 AI 处理
- 避免让 prompt 成为唯一治理手段
- 让所有 AI 结果都能落到数据库里并接受人工审核
