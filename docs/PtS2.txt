1.关于菜谱库页面的修改：
（1）“仅看BMD”、“仅看CC”这个字段名改为“BMD”、“CC”。
（2）“Web 端编辑已禁用。请修改 Excel 源文件后重新同步”这句话删掉。
（3）删除“原始标签”选单，把“系统标签”改为“自动标签”（此条也适用于所有其他页面）
（4）把“自动标签”从多选一的下拉选单，改为复选框

2.我想实现用本地AI给所有recipe打上标签，请按如下方式：1.请你首先根据原表（recipes.xslx）的内容，为我生成20-30个标签，这些标签要避开已有的专题库和分组的分类方法（否则就重复了没有意义），可以考虑类似于清淡、麻辣等分类方式。2.把你生成的标签写入数据库，作为一个数据库自带的表，无论如何导入recipes表格都不影响标签。3.在程序左侧新建一个tab，叫做标签管理，支持至少三项功能：（1）支持用户对现有标签进行管理，这个管理也是修改数据库自带的表，和recipes表格导入无关；（2）做一个按钮，点击之后即启动本地AI，逐项给现有数据库中的recipes打标签，并在此过程中显示进度（如x/y，x是已经完成了多少个打标签工作，y是总的工作量），并随时可以暂停；（3）打标签的过程开始时，先判断哪些recipe已经打过标签了，如果这些recipe和上次没有变化则自动跳过。

3.智能问答里的“当前已选菜谱”是什么？